# mtop

htop for your local AI.

One terminal dashboard for everything running on your machine: which models are loaded, how much VRAM they're eating, tokens/sec live, context-window fill, GPU temps. Works with Ollama, llama.cpp and LM Studio. Single binary, no config.

Status: design phase. See [docs/design.md](docs/design.md).
